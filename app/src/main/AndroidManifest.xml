<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.expensetracker">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".SelectedRecordDetails"
        android:allowBackup="true"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditRecordincm"></activity>
        <activity android:name=".EditRecord" />
        <activity android:name=".Historyincm" />
        <activity android:name=".Consult" />
        <activity android:name=".Setting" />
        <activity android:name=".Invest" />
        <activity android:name=".Finance" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Second" />
        <activity android:name=".Income" />
        <activity android:name=".Expense" />
        <activity android:name=".Profile" />
        <activity android:name=".History" />
    </application>

</manifest>